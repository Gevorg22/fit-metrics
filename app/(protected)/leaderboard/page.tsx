import { LeaderboardView } from '@/components/leaderboard/LeaderboardView';

export const metadata = { title: 'Рейтинг — fitMetrics' };

export default function LeaderboardPage() {
  return <LeaderboardView />;
}
